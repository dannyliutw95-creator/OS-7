Readme 這個系統分為兩個角色，透過 JSON 格式的指令來溝通： • 發送端 (COM4 / 主控部)： • 它是系統的「大腦」。 • 負責接收你輸入的指令（例如：你打入一段 JSON 代碼）。 • 負責解碼 (Decode)，把複雜的文字轉換成電腦看得懂的數值，再傳送出 去。 • 接收端 (COM3 / 執行部)： • 它是系統的「手腳」。 • 負責聽從命令，接收來自發送端的資訊。 • 將結果顯示出來（例如：顯示 JSON Count is 0x2），代表它成功同 步了狀態。
